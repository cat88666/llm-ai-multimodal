Extract every piece of visible text from this image and output it in Markdown format.

Include:
- All printed and handwritten text
- All numbers, dates, amounts, names, addresses, stamps, seals
- Table contents with Markdown table formatting
- Headers, footers, watermarks if readable

Do not describe the image. Do not add labels like "[stamp]" or "[signature]". Only output the actual text content.
